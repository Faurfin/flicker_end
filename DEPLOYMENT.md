# Руководство по развертыванию Flicker Messenger на сервере

## 📋 Что уже есть в проекте

✅ **Docker Compose** (`docker-compose.yml`) - для оркестрации контейнеров  
✅ **Dockerfile** (`backend/Dockerfile`) - для сборки backend  
✅ **Nginx конфигурация** (`nginx/nginx.conf`) - для reverse proxy и SSL  
✅ **Переменные окружения** (`.env`) - для конфигурации  

## 🚀 Пошаговая инструкция по деплою

### 1. Подготовка сервера

#### Требования к серверу:
- Ubuntu 20.04+ / Debian 11+ (рекомендуется)
- Минимум 2GB RAM, 2 CPU cores
- Docker и Docker Compose установлены
- Домен настроен и указывает на IP сервера

#### Установка Docker и Docker Compose:

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER

# Установка Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# Перезагрузка (или перелогин)
newgrp docker
```

### 2. Настройка домена

#### DNS записи:
Убедитесь, что ваш домен указывает на IP сервера:
```
A запись: yourdomain.com → IP_СЕРВЕРА
A запись: www.yourdomain.com → IP_СЕРВЕРА (опционально)
```

Проверка:
```bash
dig yourdomain.com
# или
nslookup yourdomain.com
```

### 3. Загрузка проекта на сервер

```bash
# Подключитесь к серверу по SSH
ssh user@your-server-ip

# Создайте директорию для проекта
mkdir -p ~/flicker
cd ~/flicker

# Загрузите файлы проекта (через git, scp, или rsync)
# Пример через git:
git clone <your-repo-url> .

# Или через scp с локальной машины:
# scp -r /path/to/flicker/* user@server:~/flicker/
```

### 4. Настройка переменных окружения

#### Создайте `.env` файл на сервере:

```bash
cd ~/flicker
nano .env
```

#### Содержимое `.env` для продакшена:

```env
# MongoDB - используйте ваш MongoDB Atlas или локальный MongoDB
MONGODB_URL=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB=flicker_messenger_db

# JWT и безопасность (ОБЯЗАТЕЛЬНО измените на случайные строки!)
JWT_SECRET_KEY=your_super_secret_jwt_key_here_change_me
SECRET_KEY=your_super_secret_key_here_change_me
ALGORITHM=HS256

# API ключи (ваши реальные ключи)
OPENAI_API_KEY=sk-proj-your-key-here
GIGACHAT_AUTH_BASE64=your_base64_key_here
DEEPSEEK_API_KEY=sk-your-key-here
GEMINI_API_KEY=your-key-here

# CORS - укажите ваш домен
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Безопасность
COOKIE_SECURE=true
ENABLE_HSTS=true
```

**⚠️ ВАЖНО:** 
- Используйте **сильные случайные ключи** для `JWT_SECRET_KEY` и `SECRET_KEY`
- Генерируйте их командой: `openssl rand -hex 32`

### 5. Обновление Nginx конфигурации

Отредактируйте `nginx/nginx.conf`:

```bash
nano nginx/nginx.conf
```

Замените `YOUR_DOMAIN_HERE` на ваш реальный домен (в 3 местах):
- Строка 26: `server_name yourdomain.com;`
- Строка 39: `server_name yourdomain.com;`
- Строка 41: `ssl_certificate /etc/letsencrypt/live/yourdomain.com/fullchain.pem;`
- Строка 42: `ssl_certificate_key /etc/letsencrypt/live/yourdomain.com/privkey.pem;`

### 6. Получение SSL сертификата (Let's Encrypt)

#### Установка Certbot:

```bash
sudo apt install certbot -y
```

#### Получение сертификата:

```bash
# Остановите nginx контейнер (если запущен)
cd ~/flicker
docker-compose down

# Получите сертификат
sudo certbot certonly --standalone -d yourdomain.com -d www.yourdomain.com

# Сертификаты будут сохранены в:
# /etc/letsencrypt/live/yourdomain.com/fullchain.pem
# /etc/letsencrypt/live/yourdomain.com/privkey.pem
```

#### Автоматическое обновление сертификатов:

Создайте cron задачу:
```bash
sudo crontab -e
```

Добавьте строку:
```
0 3 * * * certbot renew --quiet --deploy-hook "cd /home/user/flicker && docker-compose restart nginx"
```

### 7. Обновление docker-compose.yml для продакшена

Проверьте `docker-compose.yml` - он уже настроен правильно, но убедитесь что:

1. Порты не конфликтуют с другими сервисами
2. Переменные окружения берутся из `.env` файла
3. MongoDB URL указывает на правильную базу

Если используете MongoDB Atlas (облачную), убедитесь что:
- IP сервера добавлен в whitelist MongoDB Atlas
- Строка подключения правильная

### 8. Запуск приложения

```bash
cd ~/flicker

# Сборка и запуск контейнеров
docker-compose up -d --build

# Проверка логов
docker-compose logs -f

# Проверка статуса
docker-compose ps
```

### 9. Проверка работы

1. **Проверка контейнеров:**
   ```bash
   docker ps
   # Должны быть запущены: fastapi_backend, mongo_db, nginx_proxy
   ```

2. **Проверка логов:**
   ```bash
   docker-compose logs backend
   docker-compose logs nginx
   ```

3. **Проверка через браузер:**
   - Откройте `https://yourdomain.com`
   - Должна открыться страница регистрации/входа

4. **Проверка API:**
   ```bash
   curl https://yourdomain.com/docs
   # Должна открыться документация FastAPI
   ```

### 10. Настройка файрвола

```bash
# Разрешить HTTP и HTTPS
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# Разрешить SSH (если еще не разрешен)
sudo ufw allow 22/tcp

# Включить файрвол
sudo ufw enable

# Проверка статуса
sudo ufw status
```

### 11. Автозапуск при перезагрузке сервера

Docker Compose с флагом `restart: always` уже настроен в `docker-compose.yml`, но убедитесь что Docker запускается автоматически:

```bash
sudo systemctl enable docker
sudo systemctl start docker
```

## 🔧 Полезные команды

### Управление контейнерами:
```bash
# Остановка
docker-compose down

# Запуск
docker-compose up -d

# Перезапуск
docker-compose restart

# Пересборка после изменений
docker-compose up -d --build

# Просмотр логов
docker-compose logs -f [service_name]

# Вход в контейнер
docker exec -it fastapi_backend bash
```

### Обновление приложения:
```bash
cd ~/flicker
git pull  # если используете git
docker-compose up -d --build
```

### Резервное копирование MongoDB:
```bash
# Если используете локальный MongoDB
docker exec mongo_db mongodump --out /data/backup
docker cp mongo_db:/data/backup ~/backups/$(date +%Y%m%d)
```

## 🐛 Решение проблем

### Проблема: Nginx не может найти SSL сертификаты
**Решение:** Убедитесь что путь в `nginx.conf` правильный и сертификаты существуют:
```bash
sudo ls -la /etc/letsencrypt/live/yourdomain.com/
```

### Проблема: Backend не подключается к MongoDB
**Решение:** 
1. Проверьте `MONGODB_URL` в `.env`
2. Если MongoDB Atlas - проверьте whitelist IP
3. Проверьте логи: `docker-compose logs backend`

### Проблема: CORS ошибки
**Решение:** Убедитесь что `CORS_ORIGINS` в `.env` содержит ваш домен с `https://`

### Проблема: Порт уже занят
**Решение:** Измените порты в `docker-compose.yml` или остановите конфликтующий сервис:
```bash
sudo netstat -tulpn | grep :80
sudo netstat -tulpn | grep :443
```

## 📝 Чеклист перед запуском

- [ ] Docker и Docker Compose установлены
- [ ] Домен настроен и указывает на IP сервера
- [ ] `.env` файл создан с правильными значениями
- [ ] `nginx.conf` обновлен с вашим доменом
- [ ] SSL сертификаты получены через Certbot
- [ ] MongoDB доступна (локальная или Atlas)
- [ ] Файрвол настроен (порты 80, 443 открыты)
- [ ] Контейнеры запущены и работают
- [ ] Сайт открывается по HTTPS

## 🔒 Безопасность

1. **Никогда не коммитьте `.env` файл в git**
2. **Используйте сильные пароли и ключи**
3. **Регулярно обновляйте зависимости**
4. **Настройте автоматическое обновление SSL сертификатов**
5. **Используйте fail2ban для защиты от брутфорса**
6. **Регулярно делайте резервные копии базы данных**

## 📞 Дополнительная помощь

Если возникли проблемы:
1. Проверьте логи: `docker-compose logs`
2. Проверьте статус контейнеров: `docker-compose ps`
3. Убедитесь что все переменные окружения установлены правильно
4. Проверьте доступность портов и домена
