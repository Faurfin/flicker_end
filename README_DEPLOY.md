# 📦 Инструкция по развертыванию Flicker Messenger

## 📁 Структура файлов для деплоя

В проекте уже есть все необходимое для развертывания:

```
flicker/
├── docker-compose.yml      # Конфигурация Docker Compose
├── backend/
│   └── Dockerfile          # Docker образ для backend
├── nginx/
│   └── nginx.conf          # Конфигурация Nginx (нужно обновить домен)
├── .env                    # Переменные окружения (НЕ коммитьте в git!)
├── .env.example            # Пример переменных окружения
├── deploy.sh               # Скрипт автоматического деплоя
├── DEPLOYMENT.md           # Подробная инструкция
└── QUICK_START_RU.md       # Быстрый старт на русском
```

## 🎯 Что уже настроено:

✅ **Docker Compose** - оркестрация контейнеров  
✅ **Dockerfile** - сборка FastAPI приложения  
✅ **Nginx** - reverse proxy с поддержкой SSL  
✅ **MongoDB** - база данных (локальная или Atlas)  
✅ **Автоматический рестарт** - контейнеры перезапускаются при сбое  

## 🚀 Быстрый старт (3 шага):

### Шаг 1: Подготовка на сервере
```bash
# Установите Docker и Docker Compose
curl -fsSL https://get.docker.com -o get-docker.sh && sudo sh get-docker.sh
```

### Шаг 2: Настройка проекта
```bash
# 1. Загрузите проект на сервер
# 2. Создайте .env файл из .env.example
cp .env.example .env
nano .env  # Заполните реальными значениями

# 3. Обновите домен в nginx.conf
sed -i 's/YOUR_DOMAIN_HERE/yourdomain.com/g' nginx/nginx.conf

# 4. Получите SSL сертификат
sudo certbot certonly --standalone -d yourdomain.com
```

### Шаг 3: Запуск
```bash
# Используйте скрипт деплоя
./deploy.sh yourdomain.com

# ИЛИ вручную
docker-compose up -d --build
```

## 📝 Что нужно настроить:

1. **`.env` файл** - переменные окружения
   - `JWT_SECRET_KEY` - сгенерируйте случайный ключ
   - `SECRET_KEY` - сгенерируйте случайный ключ  
   - `MONGODB_URL` - строка подключения к MongoDB
   - `CORS_ORIGINS` - ваш домен (https://yourdomain.com)

2. **`nginx/nginx.conf`** - замените `YOUR_DOMAIN_HERE` на ваш домен

3. **DNS** - настройте A-запись домена на IP сервера

4. **SSL** - получите сертификат через Certbot

## 🔒 Безопасность:

- ✅ Используйте сильные случайные ключи для JWT
- ✅ Не коммитьте `.env` в git
- ✅ Используйте HTTPS (SSL сертификаты)
- ✅ Настройте файрвол (откройте только 80, 443, 22)
- ✅ Регулярно обновляйте зависимости

## 📚 Документация:

- **`DEPLOYMENT.md`** - подробная пошаговая инструкция
- **`QUICK_START_RU.md`** - краткая инструкция на русском
- **`.env.example`** - пример переменных окружения

## 🆘 Поддержка:

Если возникли проблемы:
1. Проверьте логи: `docker-compose logs`
2. Проверьте статус: `docker-compose ps`
3. Убедитесь что все переменные окружения установлены
4. Проверьте доступность портов и домена

---

**Готово к деплою!** Следуйте инструкциям в `DEPLOYMENT.md` для детального развертывания.
