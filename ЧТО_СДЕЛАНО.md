# ✅ Что было подготовлено для деплоя на сервер

## 📦 Созданные файлы:

### 1. **DEPLOYMENT.md** 
   Подробная пошаговая инструкция по развертыванию на сервере:
   - Установка Docker и Docker Compose
   - Настройка домена и DNS
   - Получение SSL сертификатов
   - Настройка переменных окружения
   - Запуск и проверка работы
   - Решение проблем

### 2. **QUICK_START_RU.md**
   Краткая инструкция на русском языке для быстрого старта

### 3. **README_DEPLOY.md**
   Обзорная информация о структуре проекта и файлах для деплоя

### 4. **.env.example**
   Пример файла переменных окружения с комментариями:
   - MongoDB настройки
   - JWT и секретные ключи
   - API ключи для AI ботов
   - CORS настройки
   - Настройки безопасности

### 5. **deploy.sh**
   Скрипт автоматизации деплоя:
   - Проверка зависимостей
   - Обновление nginx.conf с доменом
   - Проверка SSL сертификатов
   - Сборка и запуск контейнеров

### 6. **.gitignore**
   Игнорирование чувствительных файлов (.env, логи, кэш)

## 🔧 Обновленные файлы:

### **docker-compose.yml**
   - Добавлен `env_file: .env` для загрузки переменных окружения
   - Добавлен `restart: always` для автоматического перезапуска
   - Все переменные теперь берутся из `.env` файла
   - Добавлены переменные для всех API ключей

## 📋 Что нужно сделать на сервере:

### Шаг 1: Подготовка
```bash
# Установите Docker и Docker Compose
curl -fsSL https://get.docker.com -o get-docker.sh && sudo sh get-docker.sh
```

### Шаг 2: Загрузка проекта
```bash
# Загрузите файлы проекта на сервер
# (через git, scp, rsync или другой способ)
```

### Шаг 3: Настройка
```bash
# 1. Создайте .env файл
cp .env.example .env
nano .env

# 2. ОБЯЗАТЕЛЬНО измените:
#    - JWT_SECRET_KEY (сгенерируйте: openssl rand -hex 32)
#    - SECRET_KEY (сгенерируйте: openssl rand -hex 32)
#    - MONGODB_URL (ваша строка подключения)
#    - CORS_ORIGINS (ваш домен: https://yourdomain.com)

# 3. Обновите домен в nginx.conf
sed -i 's/YOUR_DOMAIN_HERE/yourdomain.com/g' nginx/nginx.conf

# 4. Настройте DNS (A-запись домена → IP сервера)
```

### Шаг 4: SSL сертификат
```bash
# Установите certbot
sudo apt install certbot -y

# Получите сертификат
sudo certbot certonly --standalone -d yourdomain.com
```

### Шаг 5: Запуск
```bash
# Используйте скрипт деплоя
chmod +x deploy.sh
./deploy.sh yourdomain.com

# ИЛИ вручную
docker-compose up -d --build
```

### Шаг 6: Проверка
```bash
# Проверьте статус
docker-compose ps

# Проверьте логи
docker-compose logs -f

# Откройте в браузере
# https://yourdomain.com
```

## ⚠️ Важные моменты:

1. **Домен должен указывать на IP сервера** (A-запись в DNS)
2. **Измените секретные ключи** в `.env` файле (не используйте дефолтные!)
3. **Настройте MongoDB** (локальная в Docker или MongoDB Atlas)
4. **Откройте порты 80 и 443** в файрволе сервера
5. **Не коммитьте `.env` файл** в git (он уже в .gitignore)

## 📚 Документация:

- **DEPLOYMENT.md** - подробная инструкция (читайте сначала)
- **QUICK_START_RU.md** - краткая инструкция
- **README_DEPLOY.md** - обзор проекта

## 🎯 Структура проекта для деплоя:

```
flicker/
├── docker-compose.yml      ✅ Готово
├── backend/Dockerfile      ✅ Готово
├── nginx/nginx.conf        ⚠️ Нужно обновить домен
├── .env                    ⚠️ Нужно создать на сервере
├── .env.example            ✅ Готово
├── deploy.sh               ✅ Готово
└── DEPLOYMENT.md           ✅ Готово
```

## 🚀 Готово к деплою!

Все необходимое подготовлено. Следуйте инструкциям в **DEPLOYMENT.md** для развертывания на сервере.
