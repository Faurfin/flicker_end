# 🚀 Быстрый старт - Развертывание на сервере

## Что нужно сделать (кратко):

### 1️⃣ Подготовка сервера
```bash
# Установите Docker и Docker Compose (если еще не установлены)
curl -fsSL https://get.docker.com -o get-docker.sh && sudo sh get-docker.sh
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
```

### 2️⃣ Загрузите проект на сервер
```bash
# Создайте папку и загрузите файлы
mkdir -p ~/flicker && cd ~/flicker
# Загрузите файлы через git, scp или другой способ
```

### 3️⃣ Настройте переменные окружения
```bash
# Скопируйте пример и отредактируйте
cp .env.example .env
nano .env

# ОБЯЗАТЕЛЬНО измените:
# - JWT_SECRET_KEY (сгенерируйте: openssl rand -hex 32)
# - SECRET_KEY (сгенерируйте: openssl rand -hex 32)
# - MONGODB_URL (ваша строка подключения к MongoDB)
# - CORS_ORIGINS (ваш домен: https://yourdomain.com)
```

### 4️⃣ Обновите nginx.conf
```bash
# Замените YOUR_DOMAIN_HERE на ваш домен
nano nginx/nginx.conf
# Или используйте sed:
# sed -i 's/YOUR_DOMAIN_HERE/yourdomain.com/g' nginx/nginx.conf
```

### 5️⃣ Получите SSL сертификат
```bash
# Установите certbot
sudo apt install certbot -y

# Остановите nginx (если запущен)
docker-compose down

# Получите сертификат
sudo certbot certonly --standalone -d yourdomain.com -d www.yourdomain.com
```

### 6️⃣ Запустите приложение
```bash
# Используйте скрипт деплоя
chmod +x deploy.sh
./deploy.sh yourdomain.com

# ИЛИ вручную:
docker-compose up -d --build
```

### 7️⃣ Проверьте работу
```bash
# Проверьте статус
docker-compose ps

# Проверьте логи
docker-compose logs -f

# Откройте в браузере
# https://yourdomain.com
```

## ⚠️ Важные моменты:

1. **Домен должен указывать на IP сервера** (A-запись в DNS)
2. **Измените секретные ключи** в `.env` файле
3. **Настройте MongoDB** (локальная или Atlas)
4. **Откройте порты 80 и 443** в файрволе
5. **Не коммитьте `.env` файл** в git

## 📋 Чеклист:

- [ ] Docker установлен
- [ ] Проект загружен на сервер
- [ ] `.env` файл создан и настроен
- [ ] `nginx.conf` обновлен с вашим доменом
- [ ] SSL сертификаты получены
- [ ] DNS настроен (домен → IP сервера)
- [ ] Порты 80/443 открыты в файрволе
- [ ] Приложение запущено и работает

## 🔧 Полезные команды:

```bash
# Остановка
docker-compose down

# Запуск
docker-compose up -d

# Перезапуск
docker-compose restart

# Логи
docker-compose logs -f backend
docker-compose logs -f nginx

# Обновление после изменений
docker-compose up -d --build
```

## 🐛 Если что-то не работает:

1. Проверьте логи: `docker-compose logs`
2. Проверьте статус: `docker-compose ps`
3. Убедитесь что домен указывает на сервер: `dig yourdomain.com`
4. Проверьте SSL сертификаты: `sudo ls /etc/letsencrypt/live/yourdomain.com/`
5. Проверьте файрвол: `sudo ufw status`

## 📖 Подробная инструкция:

Смотрите файл `DEPLOYMENT.md` для детальной информации.
